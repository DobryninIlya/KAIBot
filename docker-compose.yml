version: "3.9"
services:
  bot:
    build: .
    container_name: telegram-bot
    environment:
      API_TOKEN: ${API_TOKEN}
      KAI_TOKEN: ${KAI_TOKEN}
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
    restart: always